# @self-deprecated/agent-tick-shared

Shared Zod schemas, API types, constants, and request encryption helpers for Agent Tick.

Most application and integration code should import `AgentTickClient` from `@self-deprecated/agent-tick-sdk`. Use this package when you need the lower-level schemas or shared helper types directly.

See <https://docs.agenttick.sh> for API and integration docs.
